class ImagePreprocessor {
  Future<String> preprocess(String imagePath) async {
    // Placeholder para pipeline real: deskew, crop, contraste, binarización.
    // Devuelve el mismo path para MVP.
    return imagePath;
  }
}
